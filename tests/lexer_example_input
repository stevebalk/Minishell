<<$A cat>>outfile|cat>outfile|'$A$A'"fdsfsdfsdkl$A kl"'"dfskfjkl"'
<infile cat>"outfile">"outfile2"<../minishell/infile2 | cat
<infile grep "Hello World" >          outfile
<$Ainfile cat>>
<infile cat>"outfile">"outfile2"<'../minishell/infile2'
<infile cat>"outfile">"outfile2"<"../minishell/infile2"
<infile cat>"outfile">"outfile2"<'infile2'
<infile cat>outfile>outfile2<infile2
exit > outfile
exit | cat > outfile
exit | ls > outfile
grep He "llo" */*
< infile grep "Hello World this is awsome" | cat > outfile


$ echo hello >
bash: syntax error near unexpected token `newline`

$ echo hello > ${NONEXISTENT}
bash: ${NONEXISTENT}: ambiguous redirect

$ echo hello > "${NONEXISTENT}"
bash: : No such file or directory

/*
If quotes are not closed > Exit
Export variables can only use alphanum characters
$_VAR_   -- Legit
$Var-/ -- Wrong
*/

PARSER:
When a redirect with a nonexistent env variable happens bash throws an ambigious redirect error.

For example:
< $NONEXISTENT echo Hello
echo hello > $NONEXISTENT

So it's neseccary to throw an error in the parsing process if a redirection with NULL will happen.

EXPANDER 

cat $fjks	== NULL
cat "$fjks"	== ""

cat $ 
cat $3333
cat $....
cat $2aaa
cat $aa33aa
cat $$

Legit syntax for out expander:
First char after $ should be a-z or _

ERROR: 
	SYNTAX ERROR NEAR UNEXPECTED TOKEN:
		< <
		< |

	AMBIGOUS REDIRECT:
		< $NONEXISTENT



Return values:
	Return NULL:
		$NONEXISTENT
		$3 (First number)
		$333 (ignore $3 and return 33)

	Return empty string:
		"$NONEXISTENT"
		''
		""

	Return last error code:
		$?

	Return $ + Whatever after it:
		$....fdfs
		$///fdfd
		Everything that doesn't match alphanum, '_' or $?


